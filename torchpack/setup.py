from setuptools import find_packages, setup

setup(
    name='torchpack',
    version='0.2.2',
    description='A neural network training interface based on pytorch',
    packages=find_packages(),
    install_requires=[])